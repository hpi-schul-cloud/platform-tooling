---
name: Build and push images on branch

on:
  push:
    branches-ignore:
      - dependabot/**
  pull_request:
    types: [labeled]

permissions:
  contents: read

jobs:
  branch_meta:
    runs-on: ubuntu-latest
    outputs:
      sha: ${{ steps.extract_branch_meta.outputs.sha }}
    steps:
      - name: Extract branch meta
        shell: bash
        id: extract_branch_meta
        env:
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          BRANCH_SHA: ${{ github.sha }}
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "sha=$PR_HEAD_SHA" >> "$GITHUB_OUTPUT"
          else
            echo "sha=$BRANCH_SHA" >> "$GITHUB_OUTPUT"
          fi

  build_and_push_job_tools:
    if: |
      (github.actor == 'dependabot[bot]' &&
      contains(github.event.issue.labels.*.name, 'ready-for-ci') == 'true') ||
      github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    needs:
      - branch_meta
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Login to GitHub Container Registry
        uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta job-tools
        id: docker_meta_job_tools
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        with:
          images: ghcr.io/${{ github.repository }}/job-tools
          tags: |
            type=sha,enable=true,priority=600,prefix=
          labels: |
            org.opencontainers.image.title=platform-tooling job-tools

      - name: Test image exists
        run: |
          echo "IMAGE_EXISTS=$(docker manifest inspect ghcr.io/${{ github.repository }}/job-tools:${{ needs.branch_meta.outputs.sha }} > /dev/null && echo 1 || echo 0)" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        if: ${{ env.IMAGE_EXISTS == 0 }}
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

      - name: Build and push job-tools
        if: ${{ env.IMAGE_EXISTS == 0 }}
        uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7.0.0
        env:
          DOCKER_BUILD_RECORD_UPLOAD: false
        with:
          context: ./job-tools
          file: ./job-tools/Containerfile
          platforms: linux/amd64
          push: true
          pull: true
          tags: ghcr.io/${{ github.repository }}/job-tools:${{ needs.branch_meta.outputs.sha }}
          labels: ${{ steps.docker_meta_job_tools.outputs.labels }}

  build_and_push_admin_tools:
    if: |
      (github.actor == 'dependabot[bot]' &&
      contains(github.event.issue.labels.*.name, 'ready-for-ci') == 'true') ||
      github.actor != 'dependabot[bot]'
    runs-on: ubuntu-latest
    needs:
      - branch_meta
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Login to GitHub Container Registry
        uses: docker/login-action@b45d80f862d83dbcd57f89517bcf500b2ab88fb2 # v4.0.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker meta admin-tools
        id: docker_meta_admin_tools
        uses: docker/metadata-action@030e881283bb7a6894de51c315a6bfe6a94e05cf # v6.0.0
        with:
          images: ghcr.io/${{ github.repository }}/admin-tools
          tags: |
            type=sha,enable=true,priority=600,prefix=
          labels: |
            org.opencontainers.image.title=platform-tooling admin-tools

      - name: Test image exists
        run: |
          echo "IMAGE_EXISTS=$(docker manifest inspect ghcr.io/${{ github.repository }}/admin-tools:${{ needs.branch_meta.outputs.sha }} > /dev/null && echo 1 || echo 0)" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        if: ${{ env.IMAGE_EXISTS == 0 }}
        uses: docker/setup-buildx-action@4d04d5d9486b7bd6fa91e7baf45bbb4f8b9deedd # v4.0.0

      - name: Build and push admin-tools
        if: ${{ env.IMAGE_EXISTS == 0 }}
        uses: docker/build-push-action@d08e5c354a6adb9ed34480a06d141179aa583294 # v7.0.0
        env:
          DOCKER_BUILD_RECORD_UPLOAD: false
        with:
          context: ./admin-tools
          file: ./admin-tools/Containerfile
          platforms: linux/amd64
          push: true
          pull: true
          tags: ghcr.io/${{ github.repository }}/admin-tools:${{ needs.branch_meta.outputs.sha }}
          labels: ${{ steps.docker_meta_admin_tools.outputs.labels }}
